<UserControl x:Class="PatrolScheduler.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PatrolScheduler.Views"
             xmlns:viewmodels="clr-namespace:PatrolScheduler.ViewModels" 
             xmlns:view="clr-namespace:PatrolScheduler.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <view:CustomerListView DataContext="{Binding CustomerListViewModel}"/>
        <view:CustomerDetailView Grid.Column="1" DataContext="{Binding CustomerDetailViewModel}"/>
        <!--<ListView ItemsSource="{Binding CapstoneCustomers}"
                  SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                  DisplayMemberPath="CustomerName">

        </ListView>-->
        
    </Grid>
</UserControl>
